---
name: requirement-to-tad
description: "将 PRD、飞书文档、Axure 说明、评审记录、流程图、接口说明或知识库 Markdown 转换为结构化 TAD 测试分析，并拆分为 TRD 业务测试分析与 STD 系统测试分析。用户要求需求分析、测试分析、规则提炼、业务流程梳理、待确认项识别，或把非结构化需求加工成可生成测试用例的文档时使用。"
---

# 需求转 TAD 分析

把原始需求提炼成可验证、可追溯、可继续生成测试用例的 TAD 文档。开始前必须阅读 [TAD 分析规则](references/tad-analysis.md)。

## 收集上下文

优先收集与目标功能最接近的材料：

- 原始 PRD、飞书内容、Axure 导出、截图、流程图、评审记录和接口说明。
- 同模块 `src/main` 下已有需求或分析文档。
- 同业务域的 TAD、TRD、STD 示例及术语。
- 代码、接口、表结构或配置中的明确技术证据。

信息冲突时，保留原文并记录冲突，不得自行修正业务含义。

## 执行分析

1. 识别角色、入口、对象、动作、条件、分支、结果、依赖、异常、数据和状态。
2. 把复合描述拆成原子事实，逐条保留来源位置。
3. 使用“条件 -> 动作 -> 结果”改写为可验证业务规则。
4. 对条件组合使用决策表，对生命周期对象使用状态迁移，对多分支流程使用 Mermaid。
5. 将有技术证据的内容写入 STD；没有证据的内容写为 `未发现明确信息` 或列入待确认项。
6. 合并重复规则，但保留最强约束、精确枚举、阈值、提示语和角色条件。

## 生成文档

默认使用以下一级结构；目标仓库已有稳定模板时，沿用本地模板：

```markdown
# 业务背景
# TRD 测试需求分析文档
# STD 系统测试分析文档
```

至少包含业务流程、业务规则、依赖关系、技术实现证据、规则合并说明和待确认问题。让每条业务规则都能直接转成一个或多个测试断言。

## 校验结果

完成前检查：

- 每条规则都有明确条件和可观察结果。
- 每个流程分支、枚举项、状态和边界均被保留。
- 依赖项包含失败、超时、重试、降级或补偿语义；原需求未说明时标记待确认。
- Mermaid 与文字规则一致。
- 没有虚构接口、字段、路由、数据库、选择器或提示语。
- 待确认项包含上下文、疑点及其对测试设计的影响。

## 输出约束

- 保留源文档中的中文业务术语。
- 优先写入与需求相同的模块目录；例如 `src/main/.../功能.md`。
- 不静默覆盖已有文件；覆盖意图不明确时创建新文件或说明冲突。
- 在规则标题或规则正文中保留来源编号，供后续 YAML 用例的 `thought` 字段引用。
